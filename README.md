This project is still under construction and is currently unavailable.

The frontend is primarily built with TypeScript and the Preact framework (optimized for minimal memory usage), while compute-intensive tasks are handled by Rust-based WebAssembly modules (optimized for maximum execution speed).

The backend is primarily built with Rust, aiming for ultimate security and performance.
